#!/bin/bash
# If 'batcat' exists, use it. Otherwise, try to run 'bat' 
# (though if this script is running, 'bat' likely isn't elsewhere)
if command -v batcat >/dev/null 2>&1; then
  exec batcat "$@"
else
  echo "bat/batcat not found"
  exit 1
fi
