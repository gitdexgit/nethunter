#!/bin/bash

# This is for ghossty term dropdown
tmux new -s "dropdown"

# this is for ping, nethogs and you know things like that
tmux new -s "computer"

# This is for reading active reading, I read mainly books in here and like that. Usually I like to open this with ghossty
tmux new -s "Q-P-C-W-E"

# this is for ~/vaults and zk too it's useful when I need to search for permanent notes and knowledge
tmux new -s "vaults"

# this is for ~/fleet so that anything fleet related is in here also for zk too
tmux new -s "fleet"

# This is for anything config related so that all the configs are in 1 location preferably
tmux new -s "config"

# This is for aclock and the ;;t.c; espanso expansion and ;;timer; too
tmux new -s "time"


# ~/Music
tmux new -s "music"
# ~/Music
tmux new -s "music2"

# this is for pulvmixer
tmux new -s "mixer"

# this is for btops and htop and cpupower
tmux new -s "btop"

# this is for jobs I place all the jobs I want to run in here so that it's easy to like fd and bg and job commands
tmux new -s "jobs"

# this is for journalctl -f
tmux new -s "logs"

# This is for the alias Afeh and rewinds for ~/tempshots for autoshot. useful for understanding and reflecting
tmux new -s "rewind"
